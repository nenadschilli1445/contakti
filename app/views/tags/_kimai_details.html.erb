<div>
  <%= form_tag save_kimai_url_tags_path, remote: true do |f| %>
    <div class="row">
      <div class="col-lg-4">
        <div class="row mb-1">
          <div class="col-md-7">
            <label class="control-label"><%= I18n.t('kimai_details.kimai_tracker_api_url') %></label>
            <%= text_field_tag :kimai_tracker_api_url, @current_user.company.kimai_tracker_api_url, class: 'form-control', placeholder: t('kimai_details.kimai_tracker_api_url_placeholder') %>
          </div>
        </div>
        <div>
          <button type="submit" class="btn btn-default btn-sm">
                                <%= t('kimai_details.save') %></button>
        </div>
      </div>
    </div>
  <% end %>
</div>